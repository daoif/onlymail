{
  "name": "@mails/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "types": "wrangler types",
    "db:local:init": "npx tsx ../scripts/migrate-d1.ts --local",
    "db:local:migrate": "npx tsx ../scripts/migrate-d1.ts --local"
  },
  "dependencies": {
    "hono": "^4.10.2",
    "postal-mime": "^2.4.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260310.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.4"
  }
}
