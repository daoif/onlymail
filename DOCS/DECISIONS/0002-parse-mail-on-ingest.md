# 决策：邮件在收件时解析并落库

## 结论
- 邮件在 Worker 收件时用 PostalMime 解析。
- `raw`、`text`、`html`、`subject`、`source` 一起入库。

## 背景
- 管理面板需要直接查看正文。
- 如果把 MIME 解析放到浏览器里，页面会重复做重活，错误处理也更分散。

## 取舍
- 选择了什么：服务端入库前完成解析，前端只做安全渲染。
- 放弃了什么：前端收到 `raw` 后再即时解析 MIME。
