# 决策：API Key 放进 D1 settings

## 结论
- 自动化 API 的 key 不放环境变量，改成存进 D1 `settings`。
- 数据库只存哈希和预览值，新 key 明文只在轮换接口返回一次。

## 背景
- 管理面板需要展示当前 key 状态并支持在线轮换。
- Worker 环境变量不能通过应用内接口直接改写，不适合承载在线轮换能力。

## 取舍
- 选择了什么：D1 持久化 + 哈希校验 + 预览值展示。
- 放弃了什么：只靠 `API_KEY` 环境变量做静态校验。
