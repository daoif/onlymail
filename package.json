{
  "name": "onlymail",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "init": "npx tsx scripts/init.ts",
    "rebuild": "npx tsx scripts/rebuild.ts",
    "set:version": "npx tsx scripts/set-version.ts",
    "migrate:d1": "npx tsx scripts/migrate-d1.ts",
    "render:wrangler": "npx tsx scripts/render-wrangler.ts",
    "setup:github": "npx tsx scripts/setup-github.ts",
    "sync:dev-vars": "tsx scripts/sync-dev-vars.ts",
    "deploy:worker": "tsx scripts/deploy-worker.ts",
    "deploy:frontend": "tsx scripts/deploy-frontend.ts",
    "test": "pnpm test:scripts && pnpm test:worker && pnpm --dir frontend test",
    "test:scripts": "node --import tsx --test scripts/test/d1-migrations.test.ts scripts/test/d1-allowed-origins.test.ts scripts/test/d1-output.test.ts scripts/test/github-repo.test.ts",
    "test:worker": "node --import tsx --test worker/test/worker.test.ts worker/test/admin-session.test.ts worker/test/domain-reconciliation.test.ts worker/test/version-update.test.ts",
    "check:scripts": "pnpm --dir worker exec tsc -p ../scripts/tsconfig.json --noEmit",
    "build": "pnpm --dir worker run build && pnpm --dir frontend run build && pnpm --dir sdk/nodejs run build",
    "build:worker": "pnpm --dir worker run build",
    "build:frontend": "pnpm --dir frontend run build",
    "build:sdk:node": "pnpm --dir sdk/nodejs run build",
    "check:python": "python -m compileall sdk/python"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "tsx": "^4.21.0"
  }
}
